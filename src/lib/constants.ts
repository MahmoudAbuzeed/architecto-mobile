/**
 * One rep = five minutes, everywhere. The countdown, the Home card, and the
 * drill rows all read from here so the app never contradicts itself again.
 */
export const REP_BUDGET_SECONDS = 300;
